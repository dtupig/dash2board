{
  "name": "elytron-dash2board-rules-tests",
  "description": "Testes das Firestore Security Rules — caso positivo e negativo por regra (achado A-02)",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "firebase --config ../../firebase.json --project elytron-d2b-dev emulators:exec --only firestore \"mocha\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.14.0",
    "firebase": "^12.18.0",
    "firebase-tools": "^15.28.1",
    "mocha": "^11.8.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
